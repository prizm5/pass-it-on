# Infrastructure notes

This folder is reserved for local-development infrastructure, deployment templates, and operational scripts.

Current assets:

- Root `docker-compose.yml` for PostgreSQL
- Environment variable template in `.env.example`
- CI workflow under `.github/workflows`
